services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: hiberus_test
      POSTGRES_USER: hiberus
      POSTGRES_PASSWORD: hiberus
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hiberus -d hiberus_test"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:80"
    volumes:
      - ./backend:/var/www/html
    depends_on:
      db:
        condition: service_healthy
    environment:
      APP_ENV: dev
      APP_SECRET: 299acd65d4187318337f7c0561721875
      DATABASE_URL: "postgresql://hiberus:hiberus@db:5432/hiberus_test?serverVersion=16&charset=utf8"
      CORS_ALLOW_ORIGIN: "http://localhost:3000"
      DEFAULT_URI: "http://localhost:8000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    environment:
      VITE_API_URL: "http://localhost:8000"

volumes:
  db_data: